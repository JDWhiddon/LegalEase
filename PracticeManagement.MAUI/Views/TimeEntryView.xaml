<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PracticeManagement.MAUI.Views.TimeEntryView"
             Title="TimeEntryView"
             NavigatedTo="OnArrived">
    <VerticalStackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
             Padding="10"
             Text="Hours"/>
            <Entry Grid.Column="1" Text="{Binding Path=Model.Hours}"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"
             Padding="10"
             Text="Employee Id"/>
            <Entry Grid.Column="1" Text="{Binding Path=Model.EmployeeId}"/>
        </Grid>
        <Button Text="OK" Command="{Binding AddOrUpdateCommand}"/>
    </VerticalStackLayout>
</ContentPage>